target=matched_id;
      while((target+1)<end)
      {
        client.db.get(target+1).then(function(data)
        {
          to_do=data.to_do;
          count=data.count;
          client.db.set(target,{"to_do":to_do,"count":count});
          client.db.delete(target+1);
          target=target+1;
        })
      }